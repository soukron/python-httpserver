from setuptools import setup, find_packages

setup (
    name             = "python-httpserver",
    version          = "0.1",
    description      = "Simple HTTP server in Python 3.",
    packages         = find_packages(),
    install_requires = [],
)
